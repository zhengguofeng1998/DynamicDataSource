<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.zgf.springcloud.mapper.slave.OrderDao">

    <select id="getOrderById" parameterType="java.lang.Long"
            resultType="com.zgf.springcloud.domain.slave.OrderDO">
        SELECT * FROM t_order WHERE id = #{id}
    </select>

    <insert id="insert">
        INSERT INTO t_order (user_id, product_id, count, money, status)
                VALUES (#{userId}, #{productId}, #{count},#{money},#{status})
    </insert>

</mapper>